package com.suraj.document_processing_service.scheduler;

import org.springframework.stereotype.Component;

@Component
public class DocumentProcessingScheduler {

    // TODO add retry or timeout reconciliation jobs when asynchronous processing is introduced.
}
